{
  "name": "app-to-svg-projection",
  "version": "1.0.0",
  "description": "Transform repositories into visual, functional SVG spaces with pure functions",
  "main": "projector.ts",
  "scripts": {
    "test": "ts-node test-harness.ts",
    "project": "ts-node projector.ts",
    "example": "ts-node projector.ts . output.svg"
  },
  "dependencies": {
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0"
  }
}